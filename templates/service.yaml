apiVersion: v1
kind: Service
metadata:
  name: {{ include "neulander.fullname" . }}
  labels:
    {{- include "neulander.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neulander.selectorLabels" . | nindent 4 }}
