##############################################################################
# API service
##############################################################################

apiVersion: v1
kind: Service
metadata:
  name: neulander-api
  
  labels:
    app: neulander-api
spec:
  selector:
    app: neulander-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP